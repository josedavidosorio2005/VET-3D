# Modelos 3D

Coloca aqui los modelos exportados desde Blender:

- `modelo_perro.glb`: export original sin comprimir.
- `modelo_perro_draco.glb`: salida comprimida para produccion.

Ejecuta `npm run asset:draco` para generar el archivo comprimido.
